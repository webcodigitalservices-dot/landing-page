function togglePrivacy() {
  const privacy = document.getElementById("privacyContent");
  privacy.style.display = (privacy.style.display === "block") ? "none" : "block";
}

function toggleTerms() {
  const terms = document.getElementById("termsContent");
  terms.style.display = (terms.style.display === "block") ? "none" : "block";
}
